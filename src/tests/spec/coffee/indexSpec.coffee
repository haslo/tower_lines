describe "Index", ->
  beforeEach ->

  it "should have some tests in here! - This test should fail to see if everything is working", ->
    # Do some testing in here!
    expect(true).toBeFalsy()
    
  it "should initialize the App", ->
    # Do some testing in here!
    newApp = app.initialize
    expect(newApp).toBeTruthy()